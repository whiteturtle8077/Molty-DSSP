{
  "name": "molty-dssp",
  "version": "0.1.0",
  "description": "Deterministic SDET Playwright — JSON BDD-driven TypeScript test framework",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:hello": "npx playwright test tests/hello-world.spec.ts",
    "test:bdd": "node --loader ts-node/esm src/runner/index.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.7.0",
    "ts-node": "^10.9.0",
    "@types/node": "^22.0.0"
  }
}
